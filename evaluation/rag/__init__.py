"""Offline regression evaluation for Knowledge search.

Import the submodules you need directly (``evaluation.rag.dataset``,
``evaluation.rag.retrieval_eval``, ``evaluation.rag.answer_eval``); this module stays
import-light so the retrieval half does not pull in ADK or a provider SDK.
"""
